package pkg:component;

interface nexmark {
    resource bid;
    // convert-currency
    q1: func(auction: u64, price: u64, bidder: u64, date-time: u64) -> tuple<u64, u64, u64, u64>;

    q1-stream: func(s: stream<bid>) -> stream<bid>;
}

world component {
    export nexmark;
}
